import geradorConselho from './geradorConselho';

// Importar o css customizado
import './styles.scss';
// Import todos os modulos(JS) do bootstrap
import * as bootstrap from 'bootstrap';

console.log(geradorConselho());
